import styled from "styled-components";

export const Title = styled.h1`
  font-size: ${(props) => props.theme.sizes.monumental};
  text-align: left;
  margin: 0;
  line-height: ${(props) => props.theme.sizes.monumental};
`;
